// LoadingSpinner component for showing a simple loading indicator

import React from "react";

export default function LoadingSpinner() {
  return <div className="spinner" role="status" aria-live="polite" />;
}
